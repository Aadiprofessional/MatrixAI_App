cmake_minimum_required(VERSION 3.13)

# Define the project
project(MatrixAI)

# This is a dummy CMakeLists.txt file to satisfy the build system
# It doesn't actually build anything

# Set REACT_NATIVE_SVG_EXCLUDE to exclude SVG from the build
set(REACT_NATIVE_SVG_EXCLUDE TRUE) 